<%= form_with(model: job, local: true) do |form| %>
  <% if job.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job.errors.count, "error") %> prohibited this job from being saved:</h2>

      <ul>
      <% job.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :contactpersoon %>
    <%= form.text_field :contactperson, id: :job_contactperson %>
  </div>

  <div class="field">
    <%= form.label :titel %>
    <%= form.text_field :title, id: :job_title %>
  </div>

  <div class="field">
    <%= form.label :actie %>
    <%= form.text_area :description, id: :job_description %>
  </div>

  <div class="field">
    <%= form.label :artikelnummer %>
    <%= form.text_field :article_number, id: :job_article_number %>
  </div>

  <div class="field">
    <%= form.label :status %>
    <%= form.text_field :status, id: :job_status %>
  </div>

  <div class="actions">
    <%= form.submit "Opslaan" %>
  </div>
<% end %>

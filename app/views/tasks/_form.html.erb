<%= form_with(model: [job, task], local: true) do |form| %>
  <% if task.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(task.errors.count, "error") %> prohibited this task from being saved:</h2>

      <ul>
      <% task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :Omschrijving %>
    <%= form.text_area :description, id: :task_description %>
  </div>

  <div class="field">
    <%= form.label :Bestand %>
    <%= form.text_field :file, id: :task_file %>
  </div>

  <div class="field">
    <%= form.label :job_nummer %>
    <%= form.text_field :job_id, id: :task_job_id %>
  </div>

  <div class="actions">
    <%= form.submit "Opslaan" %>
  </div>
<% end %>
